# simple-financial_instruments


## how to start project

1. install uv

```sh
# create virtual env
python uv venv

# activate virtual env
source .venv/bin/activate

uv pip install -r pyproject.toml
```
